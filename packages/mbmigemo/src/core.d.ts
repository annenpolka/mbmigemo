/** Generated MoonBit ABI: instances are opaque, including invalid handles. */
export function initialize(bytes: Uint8Array): unknown;
export function is_valid(instance: unknown): boolean;
export function query(instance: unknown, input: string): string;
